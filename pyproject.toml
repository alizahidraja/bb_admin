[build-system]
requires = ["flit_core>=3.2"]
build-backend = "flit_core.buildapi"

[project]
name = "bb_admin"
version = "0.0.1"
description = "No-code branch manager facade for the restaurant POS (bb_admin)."
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Entropic Technologies PK LTD"}]
requires-python = ">=3.10"

[tool.flit.module]
name = "bb_admin"
